services:
  frontend:
    build:
      context: ./www/infestation-championship
      dockerfile: Dockerfile.prod
      args:
        VITE_API_URL: "https://api.infestationchampionship.tech/api"
    container_name: infestation-frontend-prod
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: production
    depends_on:
      - api
    networks:
      - infestation-network

  api:
    build:
      context: ./API
      dockerfile: Dockerfile.prod
    container_name: infestation-api-prod
    ports:
      - "7180:7180"
    env_file:
      - .env
    depends_on:
      sqlserver:
        condition: service_healthy
    networks:
      - infestation-network

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: sqlserver-prod
    environment:
      ACCEPT_EULA: "Y"
      SA_PASSWORD: ${SQLSERVER_PASSWORD}
    volumes:
      - sqlserver_data_prod:/var/opt/mssql
    healthcheck:
      test: ["CMD", "/opt/mssql-tools18/bin/sqlcmd", "-S", "localhost", "-U", "sa", "-P", "${SQLSERVER_PASSWORD}", "-C", "-Q", "SELECT 1"]
      interval: 3s
      timeout: 5s
      retries: 3
    networks:
      - infestation-network

networks:
  infestation-network:
    driver: bridge

volumes:
  sqlserver_data_prod:
